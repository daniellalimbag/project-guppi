# Swap these PNGs with your final art (keep the same filenames).

- icon_check.png — approve / looks real
- icon_flag.png — escalate / flag
- icon_quota_star.png — feed quota badge (assign a custom Texture2D on FeedScreen.quota_star_texture, or replace this file)
- icon_hamburger.png — unused on feed (kept for other UI if needed)
